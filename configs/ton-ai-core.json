{
  "name": "ton-ai-core",
  "version": "0.2.0",
  "description": "ton-ai-core",
  "scripts": {
    "clean": "npm run clean:core",
    "clean:core": "cd packages/core && npm run clean",
    "build:core": "cd packages/core && npm install && npm run build && npm pack",
    "rebuild": "npm run clean && npm run build:core"
  },
  "keywords": [
    "ton",
    "ai",
    "blockchain"
  ],
  "author": "testertesterov",
  "license": "MIT"
}
