{
  "name": "ton-ai-gigachat",
  "version": "0.1.0",
  "description": "ton-ai-gigachat",
  "scripts": {
    "clean": "npm run clean:core && npm run clean:gigachat && npm run clean:agent",
    "clean:core": "cd packages/core && npm run clean",
    "clean:gigachat": "cd plugins/gigachat && npm run clean",
    "clean:agent": "cd agents/gigachat && (test -d dist && rm -rf dist || true) && (test -d node_modules/@ton-ai && rm -rf node_modules/@ton-ai || true)",

    "build:core": "cd packages/core && npm install && npm run build && npm pack",
    "build:gigachat": "cd plugins/gigachat && npm install && npm run build && npm pack",
    "build:agent": "cd agents/gigachat && npm install",

    "copy-packages": "mkdir -p agents/gigachat/node_modules/@ton-ai && cp packages/core/ton-ai-core-0.1.0.tgz agents/gigachat/node_modules/@ton-ai/ 2>/dev/null || echo 'Core package copy skipped' && cp plugins/gigachat/ton-ai-gigachat-0.1.0.tgz agents/gigachat/node_modules/@ton-ai/ 2>/dev/null || echo 'GigaChat package copy skipped'",

    "rebuild": "npm run clean && npm run build:core && npm run build:gigachat && npm run build:agent && npm run copy-packages",

    "rebuild:quick": "npm run build:core && npm run build:gigachat && npm run copy-packages && cd agents/gigachat && npm install",

    "start:agent": "cd agents/gigachat && npm start",

    "dev": "npm run rebuild && npm run start:agent"
  },
  "keywords": ["ton", "ai", "gigachat", "sberbank"],
  "author": "testertesterov",
  "license": "MIT"
}
